import { StyleSheet } from "react-native";

export const stylesChoice = StyleSheet.create({
    container:{
        borderWidth: 7,
        paddingHorizontal: 15,
        borderRadius: 15,
        backgroundColor: 'rgba(0,0,0,0.2)'
    },
    text:{
        fontSize: 40,
        fontWeight: 'bold',
        textTransform: 'uppercase',
        letterSpacing: 4,
    }
});

export default stylesChoice;